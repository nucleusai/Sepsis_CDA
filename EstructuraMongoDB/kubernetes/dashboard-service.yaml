apiVersion: v1
kind: Service
metadata:
  name: dashboard
spec:
  type: NodePort
  selector:
    app: dashboard
  ports:
    - name: http 
      port: 8050
      targetPort: 8050
      nodePort: 30003
